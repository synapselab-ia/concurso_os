# NEXT_ACTION

## BASE-001 — Continuar baseline inicial de `p001` no TJSP

Estado atual do `stage_1`: **1/13 itens concluídos**. Próximo item: **2**, competência `PT.TEXT.COHESION_RELATIONS`.

Próxima execução com participação do estudante:

1. aplicar o item 2 uma questão por vez, sem revelar a resposta antes da tentativa;
2. registrar resposta e confiança antes da correção;
3. usar questão inédita por padrão e corrigir com base no próprio texto ou fonte apropriada;
4. persistir a observação como `EvidenceEvent` append-only no enrollment de `p001`;
5. repetir até concluir os 13 itens do `stage_1`;
6. não atribuir `MASTERED` com base nesse rastreio inicial;
7. ao concluir o stage 1, produzir o primeiro estado derivado e definir o stage 2 adaptativo;
8. fazer o writing screen depois do stage 1 objetivo.

O baseline exige respostas reais de `p001`; não inventar evidência para avançar o estado.
